# Log level (optional)
RUST_LOG=gemini_co_cli=debug,tower_http=debug

# Google Gemini API Key (REQUIRED for first-time setup)
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# ============================================================================
# QUICK START:
# ============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Get an API key from: https://aistudio.google.com/apikey
# 3. Paste your key after GEMINI_API_KEY= above
# 4. Run: docker-compose up
# 5. Open: http://localhost:3000
#
# ============================================================================
# AUTHENTICATION OPTIONS:
# ============================================================================
#
# Option 1 - API Key (Simpler - RECOMMENDED):
#   - Set GEMINI_API_KEY above, then run docker-compose up
#   - No additional authentication steps needed
#
# Option 2 - Login with Google (OAuth):
#   - Leave GEMINI_API_KEY empty
#   - First run: docker-compose exec gemini-co-cli gemini
#   - Select "Login with Google" from the menu
#   - Follow browser authentication prompts
#   - Credentials are cached in the gemini-config volume
#
# ============================================================================
# TROUBLESHOOTING:
# ============================================================================
# If the Gemini terminal closes immediately:
#   - Check logs: docker-compose logs gemini-co-cli
#   - Verify API key is set: docker-compose exec gemini-co-cli env | grep GEMINI
#   - Try OAuth authentication as fallback
